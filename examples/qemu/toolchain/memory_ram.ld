
MEMORY
{
    RAM_TOTAL (xrw): ORIGIN = 0x0003E000, LENGTH = 64K
}

MEMORY
{
    RAM_ACTIVE(rwx): ORIGIN = ORIGIN(RAM_TOTAL)                      , LENGTH = LENGTH(RAM_TOTAL) - 4
    SKIP_CODES (rw): ORIGIN = ORIGIN(RAM_ACTIVE) + LENGTH(RAM_ACTIVE), LENGTH = 4
}
